I"w<p>Infinite scroll to the bottom of the page which could be handy to in/on-load all of the “lazy” assets and get the entire content of the whole page</p>

<p>Set the delay time with the last number of the line, like 2000 is equal to 2s</p>

<hr />

<blockquote>
  <p>JavaScript</p>
</blockquote>

<div class="code-header">
    <button class="copy-code-button" aria-label="Copy code to clipboard"></button>
</div>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    setInterval(function() { window.scrollTo(0, document.body.scrollHeight); },2000);
</code></pre></div></div>
:ET