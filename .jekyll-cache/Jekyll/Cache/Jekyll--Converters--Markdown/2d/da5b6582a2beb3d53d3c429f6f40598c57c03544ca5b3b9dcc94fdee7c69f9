I"£<h3 id="telnet-connection---save-session-into-file">Telnet connection - save session into file</h3>

<p>You can do it by using the tee command:
<code class="language-plaintext highlighter-rouge">$ telnet $someIp | tee -a -i someFile</code></p>

<p>Example:
<code class="language-plaintext highlighter-rouge">$ telnet 192.168.1.251 | tee -a -i modem.txt</code></p>

<p>File will be saved where the terminal path was</p>
:ET